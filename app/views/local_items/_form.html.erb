<%= form_with(model: local_item) do |form| %>
  <% if local_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(local_item.errors.count, "error") %> prohibited this local_item from being saved:</h2>

      <ul>
        <% local_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :livro_id, style: "display: block" %>
    <%= form.text_field :livro_id %>
  </div>

  <div>
    <%= form.label :list_id, style: "display: block" %>
    <%= form.text_field :list_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
